{
  "name": "ai-beauty-factory-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 6070",
    "typecheck": "tsc --noEmit",
    "build": "next build",
    "start": "next start -p 6070",
    "test": "vitest run --config vitest.config.ts",
    "test:e2e:studio": "playwright test -c playwright.studio.config.ts",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "next": "16.2.1",
    "react": "^18",
    "react-dom": "^18",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
